<script type="text/javascript">
var mlisturl = "***mlist-url***";
var selects;

window.addEvent('domready', function() {
    $$("table.listtable li.info a").each(function(elem) {
        elem.addEvent("click", function(event) { event.stopPropagation(); });
    });
    selects = new SelectControl('select-ctrl');
});
</script>
<div id="queuelist">
    <div class="shadowbox">
        <h2>{L_TELLUS_QLIST_TITLE}</h2>
        <div class="sbcontent">
            <div id="messagebrowser">
                <div id="msgops"><div id="select-ctrl" class="selctrl">
                        <span class="selctrl-mode">&nbsp;</span>
                        <span class="selctrl-arr">&nbsp;</span>
                        <ul>
                            <li class="selctrl-opt" data-selctrl-mode="all">{L_TELLUS_QLIST_SELALL}</li><li class="selctrl-opt" data-selctrl-mode="none">{L_TELLUS_QLIST_SELNONE}</li><li class="selctrl-opt" data-selctrl-mode="new">{L_TELLUS_QLIST_SELNEW}</li>
                        </ul>
                    </div><ul id="message-controls" class="msgctrl">
                        <!-**targets**->
                        <li id="msgctrl-move"><img src="{V_[templatepath]}images/tellus/movequeue.png" width="16" height="16" alt="move" title="{L_TELLUS_QLIST_SETQUEUE}" /></li><li id="msgctrl-reject"><img src="{V_[templatepath]}images/tellus/reject.png" width="16" height="16" alt="reject" title="{L_TELLUS_QLIST_REJECT}" /></li><li id="msgctrl-delete"><img src="{V_[templatepath]}images/tellus/delete.png" width="16" height="16" alt="delete" title="{L_TELLUS_QLIST_DELETE}" /></li>
                    </ul>
                </div>
                <table class="listtable">
                   <tbody>
***messages***
                   </tbody>
                </table>
***paginate***
            </div>
            <div id="queuebrowser">
***queues***
            </div>
            <div class="clear"></div>
        </div>
    </div>
</div>
