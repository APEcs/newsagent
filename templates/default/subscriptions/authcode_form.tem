***error***
<div class="shadowbox subcore">
    <h2>{L_SUBS_AUTHCODE_TITLE}</h2>
    <div class="sbcontent">
        <div class="actform">
            <p>{L_SUBS_AUTHCODE_FORM}</p>
            <form id="authform" method="post" action="***target***">
                <div class="entry">
                    <label for="actcode">{L_SUBS_AUTHCODE_CODE}:<br />
                    <input type="text" id="authcode" name="authcode" class="highlight" size="64" maxlength="64" /></label>
                </div>
                <div class="submit">
                    <div class="contextlink">(<a href="***url-resend***">{L_SUBS_AUTHCODE_RESEND}</a>)</div>
                    <input type="submit" class="button blue" id="doauth" name="doauth" value="{L_SUBS_AUTHCODE_AUTH}" />
                </div>
            </form>
        </div>
    </div>
</div>
